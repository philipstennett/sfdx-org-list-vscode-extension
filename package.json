{
  "name": "sfdx-org-list",
  "displayName": "SFDX Org List",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.40.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:orgList"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "org-list",
          "title": "Org List",
          "icon": "media/dep.svg"
        }
      ]
    },
    "views": {
      "org-list": [
        {
          "id": "orgList",
          "name": "Org List"
        }
      ]
    },
    "commands": [
      {
        "command": "orgList.refresh",
        "title": "Refresh",
        "icon": {
          "light": "resources/light/refresh.svg",
          "dark": "resources/dark/refresh.svg"
        }
      },
      {
        "command": "orgList.open",
        "title": "Open",
        "icon": {
          "light": "resources/light/edit.svg",
          "dark": "resources/dark/edit.svg"
        }
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "orgList.refresh",
          "when": "view == orgList",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "orgList.open",
          "when": "view == orgList && viewItem == org",
          "group": "inline"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.11.7",
    "@types/vscode": "^1.40.0",
    "glob": "^7.1.5",
    "mocha": "^6.2.2",
    "typescript": "^3.6.4",
    "tslint": "^5.20.0",
    "vscode-test": "^1.2.2"
  }
}
